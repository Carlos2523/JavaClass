package semana.pkg2;
import java.util.Random;
import javax.swing.JOptionPane;

/**
 *
 * @author LABORATORIO 04
 */
public class Semana2 {

    /**
     * @param args the command line arguments
     * Random random = new Random();
     */
    public static void main(String[] args) {
        // TODO code application logic here
        //Inicio del ciclo for 
        double promedio = 0;
        for (int i=0; i<24;++i){
            Random random = new Random();
            promedio = random.nextInt(20000, 50000)+promedio;
            System.out.println("El valor "+(i+1)+" es igual a "+random.nextInt(20000, 50000));//Rango de numeros 
        
        //Ejercicio 1
        /*
        String read = "";
        double age;
        read = JOptionPane.showInputDialog("Digite su edad:");
        age = Double.parseDouble(read);
        if (age>=18){
            JOptionPane.showMessageDialog(null, "Puede votar");
        }  
        else{
            JOptionPane.showMessageDialog(null, "No puede votar");
        }*/
        {
        //Ejercicio 2
        /*
        String note = "";
        double note2;
        note = JOptionPane.showInputDialog("Digite su nota:");
        note2 = Double.parseDouble(note);
        if (note2>=70){
            JOptionPane.showMessageDialog(null, "Usted aprobó el curso ");
        }  
        else{
            JOptionPane.showMessageDialog(null, "Usted reprobó el curso"); 
*/
        }
    }
    
    
    }
}
